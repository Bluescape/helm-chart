apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bluescape-nxlog-ce.fullname" . }}-test-connection"
  labels:
    {{- include "bluescape-nxlog-ce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bluescape-nxlog-ce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
