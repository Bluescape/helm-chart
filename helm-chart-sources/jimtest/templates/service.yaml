apiVersion: v1
kind: Service
metadata:
  name: {{ include "jimtest.fullname" . }}
  labels:
    {{- include "jimtest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jimtest.selectorLabels" . | nindent 4 }}
