apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jimtest.fullname" . }}-test-connection"
  labels:
    {{- include "jimtest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jimtest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
